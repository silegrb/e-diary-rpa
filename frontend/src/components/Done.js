import React from 'react';
import { Check } from 'react-feather';

const Done = () => (
  <div className="w-100 h-100 d-flex justify-content-center align-items-center">
    <Check color="#FFFFFF" height={100} width={100} />
  </div>
);

export default Done;
